const name=["Batman","Robin","KnightWing"]
console.log(name); //But 'name' cannot re-assign in the whole program.

//change the names into the array list:
name[0]="joker"; //name can change.
console.log(name) 

//push elements into the array list:
name.push("HarleyQueen") //push element into the array
console.log(name)

//pop elements into the array list:
name.pop() //pop element into the array
console.log(name)
